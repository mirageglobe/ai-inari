// Package version holds the single source of truth for the application version.
// it owns nothing beyond the Version constant; all callers import this package directly.
package version

// Version is the current release version of ai-inari.
const Version = "v0.2.0"
